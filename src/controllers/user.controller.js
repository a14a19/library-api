import Users from "../models/user.model.js";

export const insertUser = async (req, res, next) => {
    try {
        // user insert query here
    } catch (e) {
        console.log("insert user: ", e)
    }
}
